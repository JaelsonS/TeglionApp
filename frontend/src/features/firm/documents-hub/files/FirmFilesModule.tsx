import { DocumentsFilesWorkspace } from '@/features/firm/documents-hub/DocumentsFilesWorkspace'

/** Ficheiros — tabela + pré-visualização (mock IMG-08). */
export function FirmFilesModule() {
  return <DocumentsFilesWorkspace />
}
