import { DocumentsHistoryWorkspace } from '@/features/firm/documents-hub/DocumentsHistoryWorkspace'

/** Histórico consolidado — tabela agregada + export CSV (mock IMG-09). */
export function FirmDocumentsHistoryModule() {
  return <DocumentsHistoryWorkspace />
}
