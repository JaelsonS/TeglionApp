# Ferramentas do monorepo

Scripts de apoio à CI e manutenção — não fazem parte do runtime da aplicação.

| Pasta | Conteúdo |
|-------|----------|
| [`ci/`](./ci/) | Verificações automáticas (limites de tamanho de ficheiros, etc.) |

Os scripts de smoke e E2E de cada app continuam em `backend/scripts/` e `frontend/scripts/`.
